# Enforce restrictive permissions on CUPS runtime config files.
# These files can contain printer device URIs and credentials — keep them
# readable by root and the cups group only, not world-readable.
z /etc/cups/classes.conf  0600 root cups - -
z /etc/cups/printers.conf 0600 root cups - -
