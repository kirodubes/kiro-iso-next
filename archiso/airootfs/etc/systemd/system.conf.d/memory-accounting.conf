[Manager]
# Enable memory accounting for all units so systemd-oomd can monitor them
DefaultMemoryAccounting=true

# Optional: Enable swap accounting (recommended for swap monitoring)
DefaultSwapAccounting=true
